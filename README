Spy on your redis databases.

Commands:
	f : move forward a page (can also use spacebar)
	b : move back a page

	k : sort by key
	t : sort by type
	l : sort by length (bytes for string, # items for others)
	v : sort by key value

	p : filter pattern

	r : refresh
	a : toggle auto-refresh

	q : quit
	? : help

